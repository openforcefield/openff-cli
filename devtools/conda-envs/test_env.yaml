name: test
channels:
  - conda-forge
  - omnia
  - openeye
dependencies:
    # Base depends
  - python
  - pip
  - click

    # OpenFF stack
  - openforcefield >= 0.7.2

    # Optional depends
  - openeye-toolkits

    # Testing
  - pytest
  - pytest-cov
  - codecov
