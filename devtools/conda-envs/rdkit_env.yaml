name: test
channels:
  - conda-forge
  - omnia
dependencies:
    # Base depends
  - python
  - pip
  - click

    # OpenFF stack
  - openforcefield >= 0.7.2

    # Optional depends

    # Testing
  - pytest
  - pytest-cov
  - codecov
