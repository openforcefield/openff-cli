name: test
channels:
  - conda-forge
  - omnia
dependencies:
    # Base depends
  - python
  - pip

    # OpenFF stack
  - openforcefield >= 0.7.2

    # Optional depends

    # Testing
  - pytest
  - pytest-cov
  - codecov
